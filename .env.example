# Docker Compose / 로컬 공통 (Git 커밋 금지 — .gitignore 의 .env)
DB_USERNAME=root
DB_PASSWORD=changeme

# 앱/DB 포트 (호스트에 노출)
SERVER_PORT=8804
MYSQL_PORT=3308

# MySQL 스키마명
MYSQL_DATABASE=plip_topic

# Redis (조회 캐시: agitUuid + date/yearMonth)
REDIS_HOST=localhost
REDIS_PORT=6379
